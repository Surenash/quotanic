version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd manfacquot
        - npm install --prefer-offline --no-audit --progress=false
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: manfacquot/dist
    files:
      - '**/*'
  cache:
    paths: []
