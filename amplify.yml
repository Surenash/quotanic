version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd manfacquot
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: manfacquot/dist
    files:
      - '**/*'
  cache:
    paths:
      - manfacquot/node_modules/**/*
